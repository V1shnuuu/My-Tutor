fastapi==0.115.*
uvicorn[standard]==0.30.*
httpx==0.27.*
numpy<2
bm25s==0.2.*
# transformers is sentence-transformers' own dependency, not imported directly here — pinned
# because newer releases (confirmed on 4.57.6, aarch64) trip a real torch._dynamo bug during
# model load ("Duplicate dispatch rule for <built-in function intern>..."): transformers'
# flex_attention integration imports torch.compiler.disable, which re-imports
# torch._dynamo.polyfills and re-registers a dispatch rule that's already registered.
# 4.44.2 is confirmed working; re-verify before bumping.
transformers==4.44.2
sentence-transformers>=3.0,<4
PyJWT[crypto]==2.9.*
python-multipart==0.0.9
pyyaml==6.*
pydantic-settings==2.*
langdetect==1.0.9
mishkal
piper-tts
yt-dlp
# Lets yt-dlp mint its own proof-of-origin token instead of relying on YOUTUBE_COOKIES_FILE —
# see deploy/setup.sh for the companion server this talks to, and CLAUDE.md #9 for why. Just
# installing this is enough: yt-dlp auto-discovers it as a plugin, no code here calls it.
bgutil-ytdlp-pot-provider
# The backend runs pipeline/ingest.py itself when an admin assigns a video (course_ingest.py),
# so it needs the same transcription stack the offline pipeline does. faster-whisper is also
# what core/app/stt.py's local lane imports; it was only ever installed by hand before.
# imageio-ffmpeg bundles a static ffmpeg binary — no system install, works on Windows and
# ARM Linux alike — which yt-dlp needs to extract audio.
faster-whisper==1.0.*
imageio-ffmpeg
